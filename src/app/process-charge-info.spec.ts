import { ProcessChargeInfo } from './process-charge-info';

describe('ProcessChargeInfo', () => {
  it('should create an instance', () => {
    expect(new ProcessChargeInfo()).toBeTruthy();
  });
});

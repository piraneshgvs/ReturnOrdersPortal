import { ProcessRequestInfo } from './process-request-info';

describe('ProcessRequestInfo', () => {
  it('should create an instance', () => {
    expect(new ProcessRequestInfo()).toBeTruthy();
  });
});

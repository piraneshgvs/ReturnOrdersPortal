import { DefectiveComponentInfo } from './defective-component-info';

describe('DefectiveComponentInfo', () => {
  it('should create an instance', () => {
    expect(new DefectiveComponentInfo()).toBeTruthy();
  });
});
